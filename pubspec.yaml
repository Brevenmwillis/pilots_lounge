name: pilots_lounge
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  go_router: ^16.0.0
  google_maps_flutter: ^2.12.3
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.3
  google_sign_in: ^6.2.1
  cloud_firestore: ^5.4.0
  image_picker: ^1.0.7
  firebase_storage: ^12.2.0
  url_launcher: ^6.2.5
  http: ^1.2.0
  syncfusion_flutter_pdf: ^24.2.9
  path_provider: ^2.1.2
  stripe_platform_interface: ^9.0.0
  stripe_android: ^9.0.0
  stripe_ios: ^9.0.0
  stripe_web: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/icons/
    - assets/images/
    - assets/faa_documents/
